#!/usr/bin/env bash
# Fix ~/.ssh permissions

set -euo pipefail

DIR="${HOME}/.ssh"
mkdir -p "$DIR"
chmod 700 "$DIR"
find "$DIR" -type d -exec chmod 700 {} \;
find "$DIR" -type f -exec chmod 600 {} \;
find "$DIR" -type f -name "*.pub" -exec chmod 644 {} \;
echo "SSH permissions fixed under $DIR"
